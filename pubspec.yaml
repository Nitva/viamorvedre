name: viamorvedre
description: "Una app de buses"
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ^3.11.4

dependencies:
  build_runner: ^2.13.1
  envied: ^1.3.4
  envied_generator: ^1.3.4
  flutter:
    sdk: flutter
  google_fonts: ^8.0.2

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
